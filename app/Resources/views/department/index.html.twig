{% extends 'base.html.twig' %}

{% block body %}
    <h1>Departments list</h1>

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for department in departments %}
            <tr>
                <td><a href="{{ path('department_show', { 'id': department.id }) }}">{{ department.id }}</a></td>
                <td>{{ department.name }}</td>
                <td>
                    {#<ul>
                        <li>#}
                            <a href="{{ path('department_show', { 'id': department.id }) }}" class="btn btn-info" >show</a>
                       {# </li>
                        <li>#}
                            <a href="{{ path('department_edit', { 'id': department.id }) }}" class="btn btn-info" >edit</a>
                        {#</li>
                    </ul>#}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {#<ul>
        <li>#}
                    <a href="{{ path('employee_index') }}" class="btn btn-info">Employee list</a>
            <a href="{{ path('department_new') }}" class="btn btn-info" >Create a new department</a>
        {#</li>
    </ul>#}
{% endblock %}
